- Make entities extend Auditable interface to automatically add audit fields.

- Apply factory pattern to all if-else chains that have to do with the parameter map source and target account ID key-value pairs.

- Consider adding links to next or previous page in the assemblers.

- Add _links to openapi example responses.

- Make UpdateMapper wire up its dependencies through constructor or setter.